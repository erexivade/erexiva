# Static image upload notes

Upload the product images for the documentation site with these exact filenames:

- `erexiva_natural.png` — used for the hero image near the top of `docs/index.rst`.
- `erexiva_blue.png` — used for the blue product image in the formula overview section of `docs/index.rst`.

Binary image files are intentionally not committed here.
